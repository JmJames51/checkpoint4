{% extends 'base.html.twig' %}

{% block title %}Family{% endblock %}

{% block body %}
    <h1>Family</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ family.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ family.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ family.lastname }}</td>
            </tr>
            <tr>
                <th>Profile</th>
                <td>{{ family.profile }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('family_index') }}">back to list</a>

    <a href="{{ path('family_edit', {'id': family.id}) }}">edit</a>

    {{ include('family/_delete_form.html.twig') }}
{% endblock %}
