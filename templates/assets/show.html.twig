{% extends 'base.html.twig' %}

{% block title %}Assets{% endblock %}

{% block body %}
    <h1>Assets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ asset.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ asset.name }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ asset.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('assets_index') }}">back to list</a>

    <a href="{{ path('assets_edit', {'id': asset.id}) }}">edit</a>

    {{ include('assets/_delete_form.html.twig') }}
{% endblock %}
